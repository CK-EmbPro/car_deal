export interface ICart {
  carName: string;
  unitPrice: number;
  quantity: number;
  carImageName: string;
  carImageCloudId: string;
  carImageCloudUrl: string;
  userEmail: string;
  userPhoneNumber: string;
}